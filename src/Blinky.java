public class Blinky extends Ghost{

    public Blinky(int x, int y, Direction direction, int section, int numSections, boolean released){
        super(x, y, direction, section, numSections, released);
    }
}
